# Advent of Zig 2025

Let's see what the hype is about. Round two with zig 0.15.2.

## Running

To run e.g. day 1 parts 1 and 2

```
zig build -Dday=1 run -- 1
zig build -Dday=1 run -- 2 # Or after the day is built just ./zig-out/bin/solution(.exe) 2
```
